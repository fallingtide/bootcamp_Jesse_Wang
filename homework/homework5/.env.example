# Copy this file to .env. These are paths (not secrets) but keeping them in
# .env makes the storage layer portable: the notebook reads them via
# python-dotenv and falls back to the defaults below if they are unset.
DATA_DIR_RAW=data/raw
DATA_DIR_PROCESSED=data/processed
